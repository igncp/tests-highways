(function() {
  window.plainJS = window.plainJS || {};
  window.plainJS.simple = {};

  var fs = window.plainJS.simple; // fs: Functions

  fs.sum = function(a, b) {
    return a + b;
  };

}).call(this);